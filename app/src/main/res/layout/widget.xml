<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
    <TextView android:id="@+id/widget_input"
              android:layout_width="match_parent"
              android:layout_height="@dimen/widget_height"
              android:layout_centerHorizontal="true"
              android:layout_centerVertical="true"
              android:gravity="center_vertical"
              android:paddingLeft="@dimen/widget_padding_horizontal"
              android:paddingRight="@dimen/widget_padding_horizontal"
              android:textSize="@dimen/widget_text_size"
              android:hint="@string/widget_text"
              android:textColorHint="@color/widget_hint_text_color"
              android:background="@drawable/widget_input"
              style="@style/RobotoMedium"/>
</RelativeLayout>